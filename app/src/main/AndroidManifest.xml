<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.barcodescanner"
    >

    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="com.example.barcodescanner.App"
        >

        <activity android:name="com.example.barcodescanner.feature.history.BarcodeHistoryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.example.barcodescanner.feature.permission.RequestPermissionsActivity"/>

        <activity
            android:name="com.example.barcodescanner.feature.scan.ScanBarcodeActivity"
            android:theme="@style/ScanQrCodeActivityTheme"
            />

        <activity android:name="com.example.barcodescanner.feature.barcode.BarcodeActivity"/>

    </application>

</manifest>